// MATERIAL UI STYLES
import { makeStyles } from '@mui/styles'

const useStyles = makeStyles((theme) => ({
  iconToggleContainer: {
    position: 'absolute',
    right: 20,
    top: 20,
    zIndex: 1,
  },
}))

export default useStyles